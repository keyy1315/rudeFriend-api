spring:
  application:
    name: rudeFriend-api
  config:
    import: "optional:file:env[.properties]"

  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: ${DATABASE_URL:jdbc:mariadb://localhost:3306/rudefriend}
    username: ${DATABASE_USERNAME:root}
    password: ${DATABASE_PASSWORD:root}
    hikari:
      maximum-pool-size: ${DB_POOL_SIZE:5}

  #  flyway:
  #    enabled: true
  #    baseline-on-migrate: true

  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:create-drop}
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MariaDBDialect
    show-sql: true
cloud:
  aws:
    s3:
      bucket: ${AWS_S3_BUCKET:test-bucket}
    region:
      static: ${AWS_REGION:ap-northeast-2}
    credentials:
      access-key: ${AWS_ACCESS_KEY:kkkkkkk}
      secret-key: ${AWS_SECRET_ACCESS_KEY:kkkkkkk}

logging:
  level:
    root: INFO
    com.lotft.nexus: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG
  pattern:
    console: "%clr(%d{yyyy-MM-dd HH:mm:ss}){faint} %clr(%5p) %clr(%logger{36}){cyan} - %msg%n"

# Swagger
springdoc:
  swagger-ui:
    enabled: true
    path: /swagger-ui/index.html
    try-it-out-enabled: true
  show-actuator: true


jwt:
  secret: ${JWT_SECRET:my-jwt-secret}
  hash-secret: ${JWT_HASH_SECRET:my-jwt-hash-secret}

server:
  port: 8081
  error:
    include-message: always
    include-binding-errors: always
    include-stacktrace: on_param
    include-exception: true
